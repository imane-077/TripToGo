<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openclassrooms.connexion">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connexion">
        <activity android:name=".resultat"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Connexion.NoActionBar" />
        <activity android:name=".Affichage_images" />
        <activity android:name=".Telechargement" />
        <activity android:name=".Photo" />
        <activity
            android:name=".navigation"
            android:label="@string/title_activity_navigation" />
        <activity android:name=".menuAdmin" />
        <activity android:name=".VisiteleSite" />
        <activity android:name=".connexionClient" />
        <activity android:name=".connexionAdmin" />
        <activity android:name=".PageCoAdCli" />
        <activity android:name=".inscription" />
        <activity android:name=".client.recherche.FormDestination" />
        <activity android:name=".Accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>